###NOTES:

#Lets decide on a model formula to run for bivariate relationships and why:
#Do we want a variable ~ varibale + year
#             variable ~ variable * year
#             variable(year1) ~ variable(year1)              